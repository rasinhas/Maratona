#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b, c, d, e, f;
    cin >> a >> b >> c;
    cin >> d >> e >> f;
    if(b*f > c*e) cout << d << endl;
    else cout << a << endl;
    return 0;
}
