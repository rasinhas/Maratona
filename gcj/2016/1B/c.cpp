#include <bits/stdc++.h>
#define ff first
#define ss second
#define mp make_pair
#define var(x) cerr << #x << " = " << x << endl;

using namespace std;

typedef long long ll;
typedef pair<int,int> pii;

map <string, int> f, s;
vector <int> v;
const int z = 1e3;

int main() {
    int T;
    cin >> T;
    for(int t=1;t<=T;t++) {
        printf("Case #%d: ", t);
        cin >> n;
        for(int i=0;i<n;i++) {
            string a, b;
            cin >> a >> b;
            if(!f.count(a)) f[a] = c;
        }
    }
    return 0;
}

