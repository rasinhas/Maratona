#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
    int a[600][600];
    int l[600];
    scanf("%d", &n);
    for(int i=0;i<n;i++) for(int j=0;j<n;j++) cin >> a[i][j];
    for(int i=0;i<n;i++) scanf("%d", &l[i]);
    return 0;
}
