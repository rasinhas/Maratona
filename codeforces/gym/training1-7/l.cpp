#include <iostream>
#include <cstdio>

using namespace std;

typedef long long ll;

/* positions
0 -> 1 top 3 right 2 front
1 -> 1 top 2 right 4 front
2 -> 1 top 4 right 5 front
3 -> 1 top 5 right 3 front
*/

int main()
{
    int r, c;
    while(cin >> r >> c && !cin.eof())
    {
        ll ans=0;
        int cy[4] = {1,3,6,4};
        int f = 2;
        for(int i=0;i<r;i++)
        {
             
            s += ;
        }
    }
    return 0;
}
