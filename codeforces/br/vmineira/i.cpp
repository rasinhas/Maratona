#include <bits/stdc++.h>
#define ff first
#define ss second
#define mp make_pair
#define var(x) cerr << #x << " = " << x << endl;

using namespace std;

typedef long long ll;
typedef pair<int,int> pii;

int a[444];

int main() {
    int n, k;
    cin >> n >> k;
    for(int i=0;i<n;i++) cin >> a[i];
    int v = n;
    while(v > k) {
    }
    return 0;
}

